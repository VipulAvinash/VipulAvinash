# Project Rules

## Git Restrictions
- **DO NOT RUN GIT COMMANDS**: Do NOT execute `git add`, `git commit`, `git push`, or any other git state-modifying commands automatically.
- The user will perform all Git staging, committing, and pushing manually.
